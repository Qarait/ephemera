# GitHub Repository Setup

When creating the repository on GitHub, please use the following details to maximize visibility:

**Description:**
> Zero-Trust SSH Certificate Authority with WebAuthn MFA and tamper-evident audit logging. Air-gap-friendly and Dockerized.

**Tags (Topics):**
- ssh
- certificate-authority
- zero-trust
- security
- webauthn
- devsecops
- docker
- cryptography

## Pushing to GitHub

Run the following commands from the `ephemera` directory:

```bash
git remote add origin <YOUR_REPO_URL>
git push -u origin main
```
